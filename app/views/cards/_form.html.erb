<div class="jumbotron">
  
  <%= simple_form_for @card do |f| %>
    <%= f.collection_select :pack_id, Pack.all, :id, :name, { promt: "Choose a Pack" } %>
    <%= f.input :original %>
    <%= f.input :translated %>
    <%= f.input :review %>
    <%= f.input :image, as: :file %>
    <%= f.button :submit  %>
  <% end %>
</div>